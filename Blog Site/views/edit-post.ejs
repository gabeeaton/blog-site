<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <link rel="stylesheet" href="../public/css/styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <style>
        .form-container {
            border: 2px solid black;
            border-radius: 8%;
            padding: 20px;
            width: 100%;
            max-width: 600px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            overflow-wrap: break-word;
        }
        button {
            border-radius: 45%;
            border: none;
            background-color: lightcoral;
            width: 50%;
        }
        label, input, textarea {
            width: 100%;
            text-align: center;
            margin-bottom: 10px;
        }
        .file-input-container {
            width: 100%;
        }
        .super-container{
            display: flex;
            justify-content: center;
            margin-bottom: 40px;
        }
        .navbar-nav .nav-link {
            color: black !important;
            opacity: 1 !important; 
        }
        .nav-link:hover{
            opacity: 0.8;
        }
    </style>
</head>
<body>
    <%- include("partials/header.ejs") %>
    <form action="/edit-post/<%= post.id %>" method="POST" enctype="multipart/form-data" class="w-100">
        <div class="super-container">
            <div class="form-container">
                <label for="title">Title:</label>
                <input type="text" id="title" name="title" class="form-control" value="<%= post.title %>">

               
                <label for="file">Upload a Photo:</label>
                <div class="file-input-container">
                    <input type="file" id="file" name="image" class="form-control">
                    <% if (post.file) { %>
                        <input type="hidden" name="existingImage" value="<%= post.file %>">
                        <p>*To change the current image, upload a new image*</p>
                    <% } %>
                </div>

                <label for="content">Content:</label>
                <textarea id="content" name="content" rows="10" class="form-control"><%= post.content %></textarea>

                <div class="d-flex justify-content-center w-100">
                    <button type="submit" class="btn btn-primary mt-3">Submit</button>
                </div>
            </div>
        </div>
    </form>
    <%- include("partials/footer.ejs") %>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
</body>
</html>
